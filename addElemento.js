// Adicione um numero no final e no início
const numeros = [10, 20, 30, 40];

numeros.push(50);//final
numeros.unshift(5);//início

console.log(numeros);

//Remover ultimo e primeiro elemento
numeros.pop();
numeros.shift();

console.log(numeros);